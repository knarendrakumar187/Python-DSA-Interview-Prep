# Interview Cheat Sheet (1 page)

## Complexity (say this)
- Loop once over n → **O(n)**
- Nested loop n*n → **O(n²)**
- Binary search → **O(log n)**
- Hash map lookup average → **O(1)**

## Python quick tools
```python
from collections import Counter, defaultdict, deque

Counter(arr)
set(arr)
sorted(arr)
enumerate(arr)
arr.append(x); arr.pop()
q = deque(); q.append(x); q.popleft()
```

## Answer formula
Clarify → Example → Brute → Better → Code → Time/Space → Edge cases

## Resume one-liners
- **Nyay Sahayak:** RAG legal assistant (ChromaDB + Groq), roles, IPC↔BNS 550+
- **GeoVerse:** multi-API exploration + LLM itinerary + fallback
- **AWS pipeline:** Lambda + Comprehend + DynamoDB, 10k+/day

## CS one-liners with example
- **Index:** faster SELECT, slower INSERT (like book index)
- **Transaction ACID:** bank transfer must fully succeed or fully fail
- **Thread vs Process:** threads share memory; processes are isolated
- **HTTPS:** HTTP + encryption (TLS)
- **JOIN:** combine rows from two tables using matching key

## Behavioural mini-template (STAR)
Situation → Task → Action (your actions) → Result (number if possible)
